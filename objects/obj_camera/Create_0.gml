//Make camera
camera = view_camera[0];
follow = obj_Character;
defaultcamwidth = camera_get_view_width(camera);
defaultcamheight = camera_get_view_height(camera);
zoomfactor = 1;
zoomrate = 0.1;
moverate = 0.1;
xgoal = x;
ygoal = y;